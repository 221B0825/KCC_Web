<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">



<mapper namespace="kosa.mapper.BoardMapper">
	<cache />

	<select id="listBoard" parameterType="Search" resultType="Board">
		select * from board
		<if test="area != null">
			<where>
				<!-- where (title LIKE %JSP% OR writer LIKE %JSP%) -->
				<foreach collection="area" item="item" open="(" close=")"
					separator="OR">
					<!-- list.jsp에 있는 검색 area의 0번째가 title, 1번째가 writer -->
					${item} LIKE #{searchKey}

				</foreach>
			</where>
		</if>
		order by seq desc
	</select>
	
	<resultMap type="Board" id="boardMap">
		<id property="seq" column="seq"/>
		<result property="title_1" column="title"/>
		<result property="writer_1" column="writer"/>
		<result property="contents" column="contents"/>
		<result property="regdate" column="regdate"/>
		<result property="hitcount" column="hitcount"/>
	</resultMap>
	
	<select id="listBoard" parameterType="java.util.Map" resultType="boardMap">
	</select>


	<select id="listBoard" parameterType="java.util.Map" resultType="Board">
		select * from board
		<if test="area != null">
			<where>
				<!-- where (title LIKE %JSP% OR writer LIKE %JSP%) -->
				<foreach collection="area" item="item" open="(" close=")"
					separator="OR">
					<!-- list.jsp에 있는 검색 area의 0번째가 title, 1번째가 writer -->
					${item} LIKE #{searchKey}

				</foreach>
			</where>
		</if>
		order by seq desc
	</select>


	<select id="detailBoard" parameterType="int" resultType="Board">
		select *
		from board where seq=#{seq}
	</select>

	<insert id="insertBoard" parameterType="Board">
		insert into board(seq,
		title, writer, contents, regdate, hitcount)
		values(board_seq.nextval, #{title}, #{writer}, #{contents}, sysdate, 0)
	</insert>

	<update id="updateBoard" parameterType="Board">
		update board set
		title=#{title}, contents=#{contents} where seq=#{seq}
	</update>
</mapper>
